# Optional public API origin for static frontend deployments.
# Example: VITE_API_BASE_URL=https://api.example.com
# Leave blank when the frontend and API are served from the same origin.
VITE_API_BASE_URL=
